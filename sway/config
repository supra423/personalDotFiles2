include ~/.config/sway/keybinds.conf
include ~/.config/sway/constants.conf
include ~/.config/sway/initialize.conf
# Default config for sway

# Read `man 5 sway` for a complete reference.

gaps inner 8
default_border pixel 3
client.focused #000000 $BORDER_FOCUSED #ffffff $BORDER_FOCUSED
client.focused_inactive #000000 $BORDER_FOCUSED #ffffff $BORDER_FOCUSED
client.unfocused #000000 #333333 #ffffff $BORDER_FOCUSED
client.urgent #000000 $BORDER_FOCUSED #ffffff $BORDER_FOCUSED

### Input configuration
#
# Example configuration:
#
input type:touchpad {
      dwt enabled
      tap enabled
      natural_scroll enabled
      middle_emulation enabled
  }
#
# You can also configure each device individually.
# Read `man 5 sway-input` for more information about this section.

include /etc/sway/config.d/*

# these programs will automatically become floating windows upon opening
for_window [app_id="pavucontrol"] floating enable
for_window [app_id="blueman-manager"] floating enable
